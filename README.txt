To run the script in Google Colab or another hosted environment:

    1. All necessary packages will be installed, and imports will be loaded after executing the "Packages" section.
    2. The entire script is ready to run; no additional steps are required.
    3. Running the code will automatically save the results.

Usage
    There are cells available for running a single test, as well as a separate cell to execute multiple scenarios and observe results.